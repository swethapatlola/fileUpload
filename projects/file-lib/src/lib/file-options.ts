export interface FileOptions {
    allowType: string;
    isMultiple: string;

    // constructor(allowType: string, isMultiple: string) {
    //     this.allowType = allowType;
    //     this.isMultiple = isMultiple;
    // }
}
